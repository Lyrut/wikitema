<?php

$form->get('text')->setAttributes([
    'class'=>'uk-input uk-width-1-2'
]);

$form->get('submit')->setAttributes([
    'class'=>'uk-button uk-button-default'
]);

$form->prepare();
?>

<div class="uk-grid-match uk-grid-small" uk-grid>
    <div class="uk-text-justify uk-grid-item-match">
        <div>
            <h1 class="uk-heading-divider"><?= $article->getTitle() ?> de <?= $creatorOfArticle->getFullName() ?></h1>
            <?= $article->getText() ?>
        </div>
    </div>
</div>


<h2 class="uk-heading-divider">Comentaires </h2>
<div class="uk-grid-match uk-grid-small">
    <div class="uk-text-justify uk-grid-item-match">
        <?= $this->form()->openTag($form); ?>
            <fieldset class="uk-fieldset">
                <legend class="uk-legend">Ajout d'un nouveau commentaire</legend>

                <?= $this->formElement($form->get('csrf')); ?>

                <div class="uk-margin">
                    <?= $this->formLabel($form->get('text')); ?> <br>
                    <?= $this->formElement($form->get('text')); ?>
                    <?= $this->formElementErrors($form->get('text')); ?>
                </div>

                <?= $this->formElement($form->get('submit')); ?>
            </fieldset>
        <?= $this->form()->closeTag(); ?>
    </div>
</div>

<?php foreach ($commentaires as $commentaire) : ?>
<div class="uk-grid-match uk-grid-small">
    <div class="uk-text-justify uk-grid-item-match">
        <div>
            <h4 class="uk-heading-divider">Comentaire de <?= $this->escapeHtml($commentaire->getUser()->getFullName()) ?> datée du <?= $this->escapeHtml($commentaire->getDate_created()) ?></h4>
            <?= $commentaire->getText() ?>
        </div>
   </div>
</div>
<?php endforeach; ?>