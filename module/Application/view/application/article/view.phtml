<?php

$form->get('text')->setAttributes([
    'class'=>'uk-input uk-width-1-2',
    'id'=>'commentaire'
]);

$form->get('submit')->setAttributes([
    'class'=>'uk-button uk-button-default'
]);

$form->prepare();
?>

<div class="uk-grid-match uk-grid-small" uk-grid>
    <div class="uk-text-justify uk-grid-item-match">
        <div>
            <h1 class="uk-heading-divider"><?= $article->getTitle() ?></h1>
            <?= $article->getText() ?>
        </div>
    </div>
</div>


<div class="uk-text-right">
    Par <?= $creatorOfArticle->getFullName() ?>
</div>
<h3 class="uk-heading-divider">Commentaires</h3>
<div class="uk-grid-match uk-grid-small">
    <div class="uk-text-justify uk-grid-item-match">
        <?= $this->form()->openTag($form); ?>
                <?= $this->formElement($form->get('csrf')); ?>

                <div class="uk-margin">
                    <?= $this->formElement($form->get('text')); ?>
                    <?= $this->formElementErrors($form->get('text')); ?>
                </div>

                <?= $this->formElement($form->get('submit')); ?>
        <?= $this->form()->closeTag(); ?>
    </div>
</div>

<?php foreach ($commentaires as $commentaire) : ?>
<div class="uk-grid-match uk-grid-small">
    <div class="uk-text-justify uk-grid-item-match">
        <div>
            <h4 class="uk-heading-divider">Commentaire de <?= $this->escapeHtml($commentaire->getUser()->getFullName()) ?> datée du <?= $this->escapeHtml($commentaire->getDate_created()) ?></h4>
            <?= $commentaire->getText() ?>
        </div>
   </div>
</div>
<?php endforeach; ?>
